# Round 03 证据协议（批次 01）

状态：本地确定性夹具已验证，实时证据待补。

现有 19 个夹具覆盖 EL-01、EL-02 和 EL-04 的边界判定：拒绝陈旧状态、诚实
no-delta 退出、权威转移、原生优先、实时状态未知时失败关闭、相同 body 与
冲突 body 区分、禁止候选直接执行，以及 curation、消费者、CALIBRATION、
ASSETS 各自独立的权威门。

这些是策略与投影夹具，不是 Agent 行为实测。它们不能证明新会话中的 Skill
加载、实时根目录优先级、延迟或上下文成本、跨宿主一致性、消费者状态、残余
缺口、自制 Skill 需要或 Hook 需要。

仍有 3 个实时证据槽：EL-01 的新会话中断/重入配对观测；EL-02 的只读根目
录、冲突和路由成本观测；消费者所有的 EL-04 投影确认。未知值继续保持未知，
不能当作缺口。

当前仍是 no Hook。本协议不授权新的外部运行、候选执行、安装、Hook 启用、
运行时变更、消费者访问、跨仓写入、标准晋升或远端 push。
