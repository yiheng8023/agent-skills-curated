# 第三轮前提挑战需求复核 — 2026-07-18

这条需求线绑定 `STM-07`、`P4` 与 `SG-03`。问题是双向的：Agent 不该对不安全、错误、
不完整或战略方向错误的人类前提照单全收，但自动服从 AI、默认人类必须推翻 AI、家长式
干预和持续盘问同样可能降低协作质量。固定在 `e060a08` 的 CALIBRATION 语料只是只读
候选证据，不是通用前提挑战算法或 Skill 规格。

现有比例化路径已经足够：默认使用原生推理；可验证事实用已授权证据工具核查；已经
批准的 `grill-with-docs` 只在用户明确要求对照文档压力测试方案时按需使用；持久且后果
重大的决策可进入规格或评审包；专业前提交给领域评审；价值和受影响主体权衡仍由负责
人类决定；开放、发散或低风险工作保留“不增加结构”的路径。12 个原生/无需 Skill
场景和 3 个文档型 curated 场景保持确定性，整体路由语料为 105/105 通过。

这没有证明残余 Skill 缺口，也不要求强制外部发现、常开 Hook、第四个核心 Skill 或
硬标准。只有反复漏掉错误前提、出现可测的过度控制成本，或出现实质不同的候选/证据时
才重新评估。
