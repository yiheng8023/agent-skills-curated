# Round 03 需求记录——第一批

日期：2026-07-15

本批次把按来源锁定的 M1 矩阵已经挑出的 4 条“可区分根因的证据通道”转成受治理的只读需求记录；不会为了凑齐历史数量而机械枚举 STM、P 或 SG 编号。

本批次绑定：

- `EL-01`：阶段切换、活跃状态、续接与诚实退出；
- `EL-02`：能力路由、激活可移植性、降级与控制成本；
- `EL-03`：证据充分性与可问责的人类/领域审查；
- `EL-04`：投影、生命周期、公私边界与跨仓治理。

每条记录继续把 STM、P、SG 作为相互独立的多对多坐标轴，并保留证据声明编号、证据状态、不确定性、保留声明、受影响主体和复查触发器。外部调研正文不会复制进本仓库。

## 当前关口

需求抽取已经开始但尚未完成。带日期的本机元数据基线现已覆盖 4 条记录，残余缺口仍全部是 `not-assessed`。只有 `EL-01`、`EL-02`、`EL-04` 具备有边界的公开元数据发现资格；`EL-03` 继续走可问责的人类/领域权威路径。

本批次不授权执行候选、准入或自制 Skill、创建或启用 Hook、提升标准、修改运行时、跨仓写入、远端推送、发布或宣称全部完成。
