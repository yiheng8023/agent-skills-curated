# Round 03 evidence protocol, batch 01

Status: local deterministic fixtures verified; live evidence pending.

Nineteen fixtures now exercise the bounded decisions for EL-01, EL-02, and
EL-04. They verify stale-state rejection, honest no-delta exit, authority
transfer, native-first routing, unknown-live-state failure, duplicate versus
conflicting bodies, direct-execution blocking, and separate curation,
consumer, CALIBRATION, and ASSETS gates.

These are policy and projection fixtures, not Agent behavior evidence. They do
not prove fresh-session Skill loading, live root precedence, latency or context
cost, cross-host parity, consumer state, a residual gap, a new Skill need, or a
Hook need.

Three live evidence slots remain: paired EL-01 fresh-session interruption and
re-entry observations; EL-02 read-only root, collision, and routing-cost
observations; and consumer-owned EL-04 projection confirmation. Unknown values
remain unknown and do not count as gaps.

The current decision remains no Hook. The protocol does not authorize fresh
external runs, candidate execution, installation, Hook enablement, runtime
mutation, consumer access, cross-repository writes, standards promotion, or
remote push.
