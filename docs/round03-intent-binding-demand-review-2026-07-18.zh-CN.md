# 第三轮意图绑定需求复核 — 2026-07-18

下一条有来源支持的需求线绑定 `STM-11`、`P1`、`P2` 与 `SG-01`：不完整或隐性的
需求表达，可能转化为意图歧义或复合请求单元遗漏。上游 CALIBRATION 语料固定在
`e060a08`，只作只读候选证据，不是 Skill 规格或标准。

对这条有边界需求，现有路径已经足够：清晰任务保留原生/无需 Skill 快路径；实质歧义
可按需使用 `intent-contract`；文档和领域语言场景可使用已经准入的
`grill-with-docs` 与 `ubiquitous-language`；重要且需要持久规格的工作进入现有
spec-driven recipe；等价且健康的官方/运行时能力可以优先；隐性目标或需负责人的判断
必须回到相应人类或领域所有者。4 个确定性场景分别覆盖 curated、native、runtime 和
安全 gap，整体路由语料继续保持 105/105 通过。

这不证明通用 intake 硬模式，也没有证明残余 Skill 缺口；不授权外部发现、候选执行、
准入、自研 Skill 或 Hook、硬标准抽取、真实环境改动、提交或推送。只有多语种或复合
请求覆盖反复失败、比例化成本恶化，或出现实质不同的候选/来源证据时才重新评估。
