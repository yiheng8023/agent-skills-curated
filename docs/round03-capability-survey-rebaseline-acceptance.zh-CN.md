# Round 03 能力调研重基线接受记录

日期：2026-07-15

所有者选择了选项 1，接受 Round 03 重基线及其按来源锁定的需求坐标输入契约。Round 03
现已针对有边界的只读能力调研激活。

授权工序依次为：提取需求记录、建立带日期的原生与 runtime 能力基线、仅针对未覆盖或不
确定需求坐标开展有边界的公开元数据发现、聚类去重、固定来源后对最少代表样本做不执行
的审查、比较备选路径，最后裁定残余缺口。

本决定只授权读取公开元数据，并且只允许在 `agent-skills-curated` 内写入经过审查的调研
证据。它不授权批量克隆、执行候选代码或 Hook、安装、连接账号、OAuth、扩展凭据或私有
数据边界、修改 live runtime、跨仓写入、Skill 准入、修改发布清单或生成路由、自制缺口
Skill、启用 Hook、晋升标准、远端推送、发布、部署或宣称全局完成。

Hook 仍只是残余缺口有充分证据成立且宿主适配性验证通过后的下游载体选项，默认不使用
Hook。

下一证据关口是在公开候选元数据发现开始之前，先形成受治理的 STM/P/SG 需求记录及带
日期的原生/runtime 基线。
